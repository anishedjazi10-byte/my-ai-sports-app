import streamlit as st
import google.generativeai as genai

# إعداد واجهة التطبيق
st.set_page_config(page_title="مدرب الذكاء الاصطناعي الرياضي", page_icon="⚽")

st.title("🤖 مدربك الرياضي الذكي")
st.write("أنا مدرب ذكاء اصطناعي، اطلب مني أي جدول تمارين أو نصيحة رياضية!")

# مكان لوضع مفتاح الذكاء الاصطناعي (سأعلمك كيف تحصل عليه مجاناً)
api_key = st.sidebar.text_input("أدخل مفتاح Gemini API الخاص بك:", type="password")

if api_key:
    genai.configure(api_key=api_key)
    model = genai.GenerativeModel('gemini-pro')

    user_input = st.text_input("مثلاً: اكتب لي جدول تمارين لتخسيس الوزن في شهر")

    if st.button("اسأل المدرب الذكي"):
        if user_input:
            with st.spinner('جاري التفكير في أفضل خطة لك...'):
                response = model.generate_content("أنت مدرب رياضي محترف. أجب على هذا السؤال: " + user_input)
                st.markdown(response.text)
        else:
            st.warning("من فضلك اكتب سؤالك أولاً!")
else:
    st.info("💡 للبدء، ضع مفتاح API في القائمة الجانبية. (سأخبرك كيف تحصل عليه مجاناً)")
